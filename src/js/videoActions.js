// "udovolstvie" - id эмоции
function starnVideo(note, context) {
    
    $response.replies = $response.replies || [];
    
    var body = {
          items: [{
            command :{
                        type:"add_note",
                        note $parseTree._anyText
                     }
                
                  }]
                };
    
    $response.replies.push({ type : "raw", body : body });

    
}